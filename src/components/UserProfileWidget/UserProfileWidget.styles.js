const userProfileWidgetStyles = {
  root: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: '50%',
    color: (theme) => theme.palette.common.white
  }
};

export default userProfileWidgetStyles;
